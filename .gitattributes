# Auto detect text files and perform LF normalization
* text=auto

# Generated text is deterministic LF on every platform. The generator's --check also
# normalizes legacy CRLF checkouts so this policy can repair them without a false stale result.
CHANGELOG.md text eol=lf
design/packages/ui/src/components/changelog/changelogData.generated.ts text eol=lf
design/packages/site/src/theme/generated/seed.ts text eol=lf

# Vitest imports this executable ESM module directly during the atomic rename tests.
# Keep it LF on Windows as well so the transform never receives a CRLF shebang/module.
scripts/finalize-worldlens-repository.mjs text eol=lf
