# Public 1.0 surface matrix

This matrix is the narrow inventory for issue [#60](https://github.com/Ding-Ding-Projects/worldlens/issues/60), “Define and document the public 1.0 compatibility contract.” It names each public-surface category in the issue acceptance checklist, assigns the owning area, and records the intended 1.0 stability boundary. The stability class is a contract target until the detailed API/reference and migration documents define the exact names, fields, versions, and failure semantics.

| Public surface category | Owner | Stability class | Contract reference | Windows-only 1.0 note |
| --- | --- | --- | --- | --- |
| CLI commands and exit codes | `design/packages/cli` | Stable for documented commands; undocumented flags remain internal | Issue #60 acceptance checklist: “CLI commands/exit codes”; [`design/README.md`](../../design/README.md) (`@worldlens/cli`) | The 1.0 desktop distribution is Windows-only. CLI compatibility must not imply a macOS or Linux installer promise. |
| Config, project, and history schemas | `design/packages/config`; `design/packages/app/src/main/config`, `project`, and `history` | Stable, versioned, and migration-required | Issue #60 acceptance checklist: “config/project/history schemas”; [`docs/config-history.md`](../config-history.md); [`docs/project-editor.md`](../project-editor.md) | Schema support is defined for the Windows 1.0 application data layout; other operating-system data locations are outside the 1.0 promise. |
| HTTP/SSE endpoints | `design/packages/server` and the app’s server/proxy boundary | Stable only for documented endpoints; private loopback and implementation endpoints are internal | Issue #60 acceptance checklist: “HTTP/SSE endpoints”; [`docs/remote-hosting.md`](../remote-hosting.md); [`docs/remote-render.md`](../remote-render.md) | The shipped 1.0 desktop endpoint consumer is Windows-only; endpoint compatibility does not promise a non-Windows desktop host. |
| JavaScript and add-on API | `design/packages/ui`, `design/packages/viewer`, and any explicitly exported package entrypoints | Internal unless an entrypoint is named in the 1.0 reference; no implicit add-on promise | Issue #60 acceptance checklist: “JS/add-on API”; [`design/README.md`](../../design/README.md) (the Java BlueMapAPI artifact is not shipped) | No non-Windows desktop add-on distribution is promised for 1.0. |
| Workflow inputs and outputs | `.github/workflows/*.yml` and the workflow-facing scripts under `design/packages/render-actions` | Stable for documented names, required inputs, output keys, artifacts, and failure states; other workflow details are experimental | Issue #60 acceptance checklist: “workflow inputs/outputs”; [`docs/release-workflow-security.md`](../release-workflow-security.md) | 1.0 release packaging and installer outputs are Windows-only; workflow render jobs must state their own runner and artifact boundary. |
| Environment variables | Build, workflow, and runtime owners: `design/packages/app/build.mjs`, `design/packages/site/vite.config.ts`, and `.github/workflows/` | Experimental until documented; only explicitly listed variables become stable | Issue #60 acceptance checklist: “environment variables”; [`design/packages/app/build.mjs`](../../design/packages/app/build.mjs) | Windows 1.0 names and path semantics are the supported contract; shell-specific or non-Windows variants are not implied. |
| File layouts | `design/packages/app/src/main/storage`, `config`, `history`, `project`, and `backup` | Stable for documented user-data, project, history, and backup locations; migrations preserve supported data | Issue #60 acceptance checklist: “file layouts”; [`docs/config-history.md`](../config-history.md); [`docs/backup.md`](../backup.md) | The 1.0 layout is the Windows application-data and filesystem layout. A layout observed on another OS is not a compatibility promise. |
| Update feed | `design/packages/app/src/main/update` and the packaging configuration | Stable for the documented feed metadata, version selection, unsigned-package warning, and rollback states | Issue #60 acceptance checklist: “update feed”; [`docs/automatic-updates.md`](../automatic-updates.md) | The 1.0 update channel publishes the Windows Squirrel.Windows installer and its update assets only. |
| Backup pointers | `design/packages/app/src/main/backup/pointer.ts` and the backup surface | Stable, with opaque pointers treated as durable references and invalid pointers rejected | Issue #60 acceptance checklist: “backup pointers”; [`docs/backup.md`](../backup.md) | Pointer resolution and application-data locations are promised for the Windows 1.0 desktop only. |
| Exported formats | `design/packages/app` export surfaces and the site/documentation export surfaces | Stable per format once listed; each format must declare its schema/version and any omitted fields | Issue #60 acceptance checklist: “exported formats”; [`docs/backup.md`](../backup.md); [`docs/config-history.md`](../config-history.md) | Windows 1.0 owns the installed-app export/import path. A portable non-Windows export consumer is not promised unless separately documented. |
| Accessibility-visible commands | `design/packages/app` UI command registry and the corresponding `design/packages/ui` surfaces | Stable command names, roles, keyboard routes, and accessible state descriptions; visual wording may localize | Issue #60 acceptance checklist: “accessibility-visible commands”; [`design/packages/app/test/screenshots.spec.ts`](../../design/packages/app/test/screenshots.spec.ts) | The 1.0 accessibility contract covers the Windows desktop UI and its keyboard conventions; other platform conventions are outside scope. |

## Classification rule

Stable means a documented consumer may rely on the named surface through 1.0, with additive changes preferred and breaking changes requiring a migration or a major-version boundary. Experimental means the surface may change between builds and must not be used as a compatibility promise. Internal means the surface exists for the product’s own implementation and is not a supported integration boundary. Deprecated means the surface remains temporarily available only with a removal path and migration guidance; no issue-60 row is currently classified as deprecated.

The detailed contract still needs to define the exact semantic-versioning, schema-migration, rollback, supported-version, security-support, and release-channel rules required by issue #60. This matrix deliberately records the inventory and ownership without claiming those unresolved details are already shipped.
