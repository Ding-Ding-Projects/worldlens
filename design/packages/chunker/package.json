{
    "name": "@worldlens/chunker",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "World conversion engine: edition and version detection, block mapping, region pruning and world settings editing",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "vitest run",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@worldlens/nbt": "workspace:*",
        "@worldlens/worldgen": "workspace:*"
    }
}
