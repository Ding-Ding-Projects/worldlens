{
    "name": "@worldlens/server",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "BlueMap server port: service, config, HTTP server, live data, commands, addon API",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "vitest run",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@worldlens/engine": "workspace:*",
        "@worldlens/nbt": "workspace:*",
        "@worldlens/shared": "workspace:*"
    },
    "devDependencies": {
        "@worldlens/worldgen": "workspace:*",
        "@types/pngjs": "^6.0.5",
        "pngjs": "^7.0.0"
    }
}
