# Session completeness audit — "finish everything I asked for before"

Written by a Sonnet auditor, read-only over the whole repository except this one file.
Every verdict below was checked against the tree at commit `c02e867` (branch `main`), the
current test files, the current hosted-issue board, and current hosted CI run state — not
against any agent's prior claim of "done." Where an earlier commit message or `HANDOFF.md`
entry already stated a caveat, that caveat is repeated here rather than re-discovered, and
credited to the entry that recorded it first.

**Read this first, because it applies to almost every row below:** at the moment of this
audit, hosted CI has a live backlog. The last hosted `CI` workflow run that actually finished
green is `31023005393` at commit `c533c8c` (2026-08-05T15:58:42Z). Fourteen commits landed on
`main` after that — `2b04a82` through `c02e867` — and every one of their `CI` runs is still
`queued` or `in_progress` as of this writing; none has finished. That block covers items 5,
6 (the EULA-export fix), 19, 20, 21, 22, 23, 24 below. Locally, the specific test files each
of those changes added or touched were run directly with `vitest run` and passed (see each
row), so the *code* is verified — the *hosted, from-a-clean-checkout* proof that HANDOFF.md's
own "how to verify things yourself" section asks for is still in flight, not failed and not
skipped. Re-run `gh run list --branch main --workflow CI --limit 15` before trusting any of
those rows as hosted-CI-green.

Also note: this repository is being edited by other agents concurrently with this audit
(the task that dispatched this audit named three live lanes explicitly out of scope for
editing). A `git status` taken partway through this audit showed working-tree changes to
`design/packages/ui/src/components/settings/DockedSurface.vue`,
`design/packages/ui/src/components/tabs/TabStrip.vew`, `design/packages/ui/src/components/menuSearch/menuCoverage.test.ts`,
and several `design/packages/site/src/content/*` files that were not there when this audit
began. None of those are this file, and this file makes no claim about their content —
they belong to the live lanes' owners to commit and report.

## The table

| # | Request | Verdict | Evidence |
|---|---|---|---|
| 1 | Hand off remaining handoff work to Sonnet agents | **DONE** | `HANDOFF.md`'s own dated entries describe repeated multi-agent passes ("Many agents worked on this repository at once, on `main`, pulling before every commit rather than using branches" — the 2026-08-05 "large concurrent pass" entry), and the commit history from `250e7e7` through `c02e867` (100+ commits) is consistent with sequential/parallel agent dispatch rather than one continuous session. This very audit is itself a delegated Sonnet subagent. |
| 2 | Correct the stale render docs (twelve waves / 3,072 shards / measured-not-assumed disk) | **DONE** | `docs/render-in-actions.md:28-30` states "the plan splits to stay under it, and a world that needs more shards than the twelve declared waves can hold fails"; line 345: "The workflow declares twelve wave jobs, so 3,072 shards." `.github/workflows/render-world.yml:173-212` declares `wave1-shards` through `wave12-shards`/`wave12-needed` outputs — twelve, counted. Disk: `docs/render-in-actions.md:374-409` ("Disk: measured, not assumed") and `.github/workflows/render-world.yml:462-482` run a real `df`-based check (`Check the runner has enough free disk for this plan`) against `design/packages/render-actions/src/plan/disk.ts`'s estimate, with the real measured numbers from a hosted run (`~6 GiB` required vs `~84 GiB` free) quoted in the doc. |
| 3 | Browse-for-folder and browse-for-file on every path text box | **DONE** | `design/packages/ui/src/components/PathField.vue` calls both `bridge.pickFolder(...)` and `bridge.pickFile(...)`. `design/packages/ui/src/components/pathFieldPolicy.test.ts` is a self-enforcing two-part guard: a named list of every wired field checked against the real source, plus a structural sweep of every `v-text-field`/`v-textarea` in the whole `ui` package whose label reads as a filesystem location, failing on any that lacks either the affordance or a written, checked exemption (`not-a-location` / `own-browse` / `remote-machine`, each independently verified). Ran locally: **9/9 tests pass**. |
| 4 | Make forms guided everywhere (assume the user does not know what to type) | **DONE** | Distributed rather than centrally guarded, but consistent: `explainField.ts` + `configExplainCoverage.test.ts` (explanation/default on every config field, see #9), `speedLevels.ts` (novice dial replacing two raw values, see #8), `presets.ts`/`presets.test.ts` (blank-slate defaults, see #7), and the accounts/CI-render picker commit `44e8453`'s own commit message names "Guided-UX states covered: nobody signed in ... exactly one account ... several accounts ... five-level FACTS pattern" as a deliberate, repeated pattern. The durable rule itself is recorded in `agent-global-memory` (`memory/SHARED_INSTRUCTIONS.md`'s guided-forms section, referenced from this project's own memory notes), confirming it was treated as a standing constraint rather than a one-off. No single test proves *every* form is guided (unlike #3, #9, #15 which have a structural sweep) — this is the one "DONE" verdict in this table resting on breadth of example rather than one exhaustive guard. |
| 5 | Resizable panels, draggable when floating | **DONE (hosted CI pending)** | `design/packages/ui/src/components/settings/DockedSurface.vue` implements a docked splitter handle ("The docked splitter: one handle on the panel's free edge, draggable with the keyboard too") and a separate floating-panel drag-by-header path ("Floating panel: drag its header to move it"). `DockedSurface.resize.test.ts` — **18/18 pass locally**. Docked-panel scrolling was fixed in `2b04a82` ("Fix docked panels not scrolling: floating panels had no real height, and the body's flex chain to nested content was broken") — see #21. `2b04a82`'s hosted `CI` run (`31025400460`) is still `pending`/queued as of this audit. |
| 6 | Multi-account support with a searchable account list box | **DONE** | `design/packages/ui/src/components/github/GitHubAccountsList.vue`: real `role="listbox"`/`role="option"` pair, roving tabindex, a `ConfigSearchField` wired to the shared regex engine (`accountSearchText`, `createSettingMatcher`), per-row switch/refresh/sign-out actions, sign-out confirmed inline, revocation status reported honestly. `githubAccountsStore.test.ts` — **15/15 pass locally**. Extended to the CI-render surface in `44e8453` (see #22) without disturbing the global active account, by design (see that commit's own message, quoted under #22). |
| 7 | Presets and default settings for blank-slate editors | **DONE** | `design/packages/ui/src/copy/surfaces/presets.ts` + `presets.test.ts`; `MapPreset` is threaded through `configWorkspace.ts`, `projectModel.ts`, `wizardModel.ts`, `MapsScreen.vue`, `ProjectMapsPanel.vue`, `paletteCatalog.ts` and `appearanceStore.ts` — used at both the map-creation wizard and the appearance editor, not a single isolated screen. |
| 8 | A novice Speed 1-5 control | **DONE** | `design/packages/ui/src/components/config/speedLevels.ts` maps a 1-5 novice dial onto BlueMap's two raw `core.conf` values (`render-thread-count`, `render-thread-priority`), documents that the calibration is this app's own invention rather than an upstream concept, and pins level 3 to both raw defaults exactly so a fresh `core.conf` reads "Balanced" rather than "Custom." `SpeedControl.vue` renders it. `speedLevels.test.ts` — **11/11 pass locally**, including a check that level 3 matches the schema's own defaults directly so the two cannot drift apart silently. |
| 9 | Explanation + default provenance on EVERY config element | **DONE** | `design/packages/ui/src/components/config/configExplainCoverage.test.ts` is a purpose-built completeness guard, not a spot check: it covers the 155 settings `configSearch.test.ts` counts (rendered via `ConfigField.vue`'s explanation + disclosure + default-provenance trio) **plus** the two places that count structurally cannot reach — a render mask's own shape fields (`MASK_SHAPES`) and a marker set's own container properties (`MARKER_SET_FIELDS`) — against an explicit, hand-maintained manifest that fails loudly if the schema grows a field the manifest doesn't know about. The test file's own doc comment names the exact prior gap this was written to close (`ConfigMarkerSetsField.vue` rendering four properties with no `FieldMeta` behind them at all). Ran locally: **12/12 pass**. |
| 10 | A full in-app docs browser | **DONE** | `design/packages/ui/src/components/docs/DocsPage.vue` is mounted at `App.vue:696` (`<DocsPage />`), reachable, not merely built — the exact "built, tested, unreachable" failure `HANDOFF.md` names as a recurring defect elsewhere in this project. Fixed for a real ellipsis-truncation bug in `7dbfc17` (index and search-result titles losing overflow text with no recovery route). That fix commit's own hosted CI run is queued, not yet green (see the top-of-file caveat). |
| 11 | Shift+right-click for appearance so it does not collide | **DONE** | `design/packages/ui/src/components/tabs/TabStrip.vue` implements `Shift+right-click` as a direct route to the appearance editor distinct from the normal right-click context menu (`event.shiftKey` branches at lines ~400-520 and ~800-825, with explicit comments: "Shift+right-click is the direct route to the appearance editor," "the menu-driven route and the Shift+right-click route land in the same place"), and `AppearanceTarget.vue` implements the matching keyboard convention (`ContextMenu` key / `Shift+F10`) for keyboard-only users. |
| 12 | Move-into-group as a searchable picker dialog | **DONE** | `design/packages/ui/src/components/tabs/TabGroupPicker.vue`: a non-modal surface with a `ConfigSearchField` wired to the shared regex builder, a list of every other group, "never a dead end" behaviour on an empty strip or a search that matches nothing, and a focus trap that also covers the search field's own regex popover. Recorded as a durable rule in `agent-global-memory` (commit `a85a63a`, "Require a picker, not an inline menu list, for moving a tab into a group"), confirming it replaced an earlier inline-menu approach rather than being new from scratch. |
| 13 | Notification centre bulk actions | **DONE** | `design/packages/ui/src/components/notifications/noticeBulk.ts`: selection model (click/shift-click/invert/scoped select-all), an honest preview that reports `changingCount` separately from `selectedCount` when they can legitimately differ, and real bulk actions (dismiss/restore/mark-as-read) run against the same `NoticeState` the single-item actions use. `noticeBulk.test.ts` — **28/28 pass locally**. Its own doc comment names the exact prior gap: "a review surface with fifty entries and no way to act on more than one of them." |
| 14 | A search bar on every right-click context menu | **DONE** | `design/packages/ui/src/components/menuSearch/menuCoverage.test.ts` is a dedicated coverage guard (**13/13 pass locally**) alongside `MenuSearchList.vue`/`MenuSearchBar.vue`, used across `SettingsMenu.vue`, `MapsMenu.vue`, `EulaViewer.vue`, `MarkerSearchField.vue`, `TabGroupPicker.vue`/`tabGroupPicker.ts`, and `ConfigApplyDialog.vue`. Note: this specific test file is one of the ones showing as locally modified by a concurrent live-lane agent as of this audit — re-run it before trusting the count if picking this up later. |
| 15 | Every search bar carries the regex builder | **DONE** | `design/packages/ui/src/components/config/regexPolicy.test.ts` — **6/6 pass locally** — is the standing policy guard `HANDOFF.md` itself cites ("Every search bar carries the anchored regex builder, kept true by `components/config/regexPolicy.test.ts` rather than by remembering"). `ConfigRegexBuilder.test.ts` covers the builder itself. |
| 16 | Non-clickable buttons / undismissable dialogs / UI bugs hunted and fixed | **DONE, ongoing by nature — several fixes not yet hosted-CI-green** | This is an open-ended hunt, not a single deliverable, so "done" here means "substantial, evidenced work landed," not "no bug remains." Real, specific bugs found and fixed this session: the whole app wearing a hand cursor (`01d21eb`), docked panels not scrolling (`2b04a82`), four separate silent-ellipsis/overflow bugs (`df1037d`, `7601828`, `7dbfc17`, `d7cda3b`), a Cantonese caption overlapping its own tick label (`8e2c44b`), a bottom-left FAB stack painting over page text (`26d74a8`), EULA export rows dimming with no stated reason (`c02e867`), and — earlier in the pass — a Vuetify overlay scrim permanently blocking the Screenshots CI job because two `.mb-eula` elements existed in the DOM at once (`3dc7ef5`, `9d8de68`), plus an appearance-editor tab strip collapsing to zero height under `overflow: hidden` and a changelog-viewer capture assuming the wrong active tab (both fixed inside `9d8de68`). All of the 2026-08-05T16:3x-16:5x commits in this list are still on queued/in-progress hosted CI runs (see top-of-file caveat) — locally, the individual fixes were spot-checked (cursor, docked-scroll, tabGroupPicker) and passed. |
| 17 | Make the Java renderer the default | **DONE** | This was already the state entering this session and remains so: Decision D17 in `design/docs/decisions.md` ("the app ships and uses the original Java engine until the TypeScript mesher passes the gate") is reaffirmed repeatedly in `HANDOFF.md`'s "What does not work yet" section as of the newest entry ("The Java engine still renders locally, by design (decision D17)... nothing has switched the desktop app's default local-render path over"). `RenderEngineId = "upstream-java" \| "typescript"` in `design/packages/app/src/main/render/provenance.ts`, with `"upstream-java"` the resolved default throughout `orchestrator.ts`. The TypeScript engine passing the byte-exact gate (a separate, already-closed milestone) was deliberately **not** treated as authorization to switch the default — that switch is named as explicitly out of scope everywhere it's mentioned. |
| 18 | Fix and close all GitHub issues | **DONE, with one documented inconsistency worth fixing** | `gh issue list --state open` returns **zero** open issues. All 34 numbered issues (`#3`-`#47`, several numbers absorbed by contracts closed earlier) are closed, each against cited evidence (test counts, real Docker servers, real hosted CI runs, real render dispatches) rather than a bare claim — spot-checked issue `#44`'s full comment thread, which shows a real 422 reproduced against a real temporarily-privatized repository, not a description of one. **The inconsistency:** `HANDOFF.md`'s own top "Plain-language summary" (the section the file says to read first) still states, as of the current commit, that "the private-repository 403 → 'needs a paid plan' mapping is untested" — but issue `#44`'s later comment ("✅ Sub-item 1 settled — the private-repo 403/422 mapping, driven for real — 2026-08-05T06:44:00-04:00") proves this was tested for real and is why the issue was closed. This is a direct, checkable violation of this project's own recorded durable rule (`agent-global-memory`'s `memory/projects/material-bluemap.md`: "Update that summary in the same commit as any new handoff entry — a handoff whose summary lags its detail sections is actively misleading, not just incomplete"). The issue itself is genuinely closed and resolved; the *summary paragraph describing it* is stale. |
| 19 | Replace every screenshot in the repo | **PARTIAL** | `HANDOFF.md`'s own "test-and-capture pass" entry accounts for 61 (Playwright re-capture) + 8 (installed-Squirrel-build capture) + 2 (live-Pages capture) = 71 of the 82 tracked PNGs under `docs/screenshots/`. **Six were not replaced**: `render-1-wizard-world.png` through `render-6-map.png`, all last modified 2026-08-04 14:38-14:45 and untouched by every subsequent refresh commit (confirmed: `git log -- docs/screenshots/render-*.png` shows no commit after `c37c2be`, which predates this session's refresh passes). The pass's own reasoning, recorded in `HANDOFF.md`: they require the app's own Mojang-download consent to reach honestly, a message claiming to relay that consent arrived mid-task from a source that was not the user and was correctly declined per this project's own rule that no agent message substitutes for real user consent, and the pass recommended **removing** the six files (since no tracked doc references them) rather than leaving them stale — but did not act on that recommendation either. **Net result: six screenshots in the repository right now are neither current nor removed**, sitting in a state the project's own documentation calls out as undesirable. |
| 20 | A full UI test and screenshot replacement again | **PARTIAL — same specific gap as #19** | This item reads as the follow-up/repeat of #19 within the session, and it was substantially executed: `HANDOFF.md`'s "Update, 2026-08-05 — a full test-and-capture pass" entry documents a second, independent sweep that found and fixed two real product bugs by chasing flaky-looking test failures rather than dismissing them (a `resume.test.ts` disk-contention timeout given its own budget in `6238074`, and a genuine `BackupRunner` resume bug that silently re-uploaded every part of an already-uploaded backup, fixed in `c533c8c` with a two-clocks-90-seconds-apart regression test). Full suite at that point: **478/478 test files, 7388/7392 tests, 4 pre-existing skips, 0 failures**, reproduced clean twice under full contention. The same six `render-*.png` gap from #19 carries over unresolved into this pass too — it was named again rather than closed. |
| 21 | Docked panels scrollable | **DONE (hosted CI pending)** | `2b04a82` ("Fix docked panels not scrolling: floating panels had no real height, and the body's flex chain to nested content was broken"). `dockScrollChain.test.ts` — **4/4 pass locally**; `DockedSurface.resize.test.ts` — **18/18 pass locally**. `2b04a82`'s own hosted `CI` run (`31025400460`) is still queued/pending as of this audit — see the top-of-file caveat. |
| 22 | An account picker for CI render | **DONE** | `44e8453` ("Add a per-render account picker to CI render setup, no active-account switch"). The "no active-account switch" phrase in the commit's own title names a deliberate design decision, not a shortfall: the picker reuses the multi-account store from #6, threads an optional account id through `cirender:owners`/`cirender:preflight`/`cirender:start`, and resolves the token in the main process via `GitHubAccountsController.accessTokenFor(id)` — but never calls `setActive`, so choosing a different account for one render does not silently change which account Settings, downloads or backups resolve to elsewhere in the app. The commit message explicitly enumerates the guided-UX states covered (nobody signed in / exactly one account / several accounts). Hosted CI for this commit (`31026651603`) is `in_progress`, not yet confirmed green. |
| 23 | The app-wide mouse-click cursor | **DONE (hosted CI pending)** | `01d21eb` ("Fix the whole GUI wearing a hand cursor: answer Vuetify's `[aria-controls]` rule at the appearance wrapper"). `design/packages/ui/src/cursorAffordance.test.ts` — **5/5 pass locally**. Hosted `CI` run (`31026199210`) is queued, not yet confirmed green. |
| 24 | Clipping and further UI bugs | **DONE, ongoing by nature — same hosted-CI-pending caveat as #16** | Same evidence list as #16 (ellipsis/overflow: `df1037d`, `7601828`, `7dbfc17`, `d7cda3b`; visual overlap: `8e2c44b`, `26d74a8`; unexplained-dim rows: `c02e867`). All from the same still-queued hosted CI block. |
| 25 | A screenshot test to verify visually | **DONE** | `design/packages/app/test/screenshots.spec.ts` runs as the hosted "Screenshots" CI job and last finished green as part of run `31013825875` at commit `9d8de68`, the run `HANDOFF.md` calls "the first fully green hosted CI run of this entire multi-agent pass." One honestly-recorded open finding survives inside it, named rather than hidden: `installed-app-1920x1200.png`'s markers panel correctly showed "Landmarks — 3 markers" with real coordinates, but the marker geometry itself did not visibly render in frame despite navigating to each marker's position — `HANDOFF.md` records this as an open question, and the image's alt text was corrected to describe what it actually shows rather than what was intended. This is this file's own live lane in the current task (excluded from edits here), so its current committed state should be re-verified by that lane's owner before being treated as unchanged. |
| 26 | Record the durable rules in global memory and push them | **DONE** | `C:/Users/cntow/Documents/GitHub/agent-global-memory` — `git status` shows the local `main` branch up to date with `origin/main` (both at `f98d025`); `git log origin/main -1` confirms the same commit is on the remote. Relevant commits present and pushed: `69ede9b` ("Add ten durable rules: guided forms, novice knobs, presets, multi-account forges, offline docs, panel resize, bug-hunt pass"), `a85a63a` ("Require a picker, not an inline menu list, for moving a tab into a group"), and `da25466` ("Promote five session lessons to shared instructions; add material-bluemap project notes"). `memory/projects/material-bluemap.md` exists with project-specific notes covering the Windows-only/Linux-CI split, the handoff-plain-language rule (see #18's finding — the rule is recorded but was not followed on the current `HANDOFF.md` tip), the Pages-hosting proof-repo reuse rule, and the copy-catalogue trap. |

## Work list — everything that is PARTIAL or NOT DONE

Nothing in this session's 26 requests came back **NOT DONE** outright; the gaps found are all
**PARTIAL**, plus one documentation inconsistency flagged under an otherwise-DONE item. In
priority order:

1. **(#19, #20) Six orphaned screenshots.** `docs/screenshots/render-1-wizard-world.png`
   through `render-6-map.png` are stale (last touched 2026-08-04, before this session's two
   refresh passes) and referenced by no tracked document. Either: (a) exercise the app's real
   Mojang-download consent flow from an actual user session and re-capture all six against the
   current build, or (b) remove the six files and their `manifest.json`/`captions.md` entries,
   per the recommendation `HANDOFF.md` already recorded but never acted on. Do not invent a
   third option (a mocked consent) — this project's own instructions already ruled that out
   once during this pass.
2. **(#18) `HANDOFF.md`'s plain-language summary is stale on issue #44.** The "What works
   right now" caveat still reads "the private-repository 403 → 'needs a paid plan' mapping is
   untested," which was true when written but is now contradicted by issue `#44`'s own later,
   closing comment thread (a real repository was privatized, a real 422 was reproduced through
   the app's own code path, and the repository was flipped back public afterward). Update that
   paragraph in the same commit as this fix, per the project's own recorded rule in
   `agent-global-memory`.
3. **(#5, #6-EULA-fix, #19, #20, #21, #22, #23, #24) Fourteen commits (`2b04a82` through
   `c02e867`) have no finished hosted CI run yet.** Every one of their `CI` workflow runs was
   still `queued` or `in_progress` at the time of this audit, behind the last confirmed green
   run (`31023005393` at `c533c8c`). The underlying code and its own new/changed unit tests
   were verified locally and passed in every case checked, but the hosted, clean-checkout,
   full-suite-plus-installer-plus-Screenshots proof `HANDOFF.md` asks agents to trust has not
   landed. Whoever picks this up next should run `gh run list --branch main --workflow CI
   --limit 15`, wait for (or investigate a stall in) the backlog, and record the first genuinely
   green run at or after `c02e867` before describing any of those seven items as
   hosted-CI-verified — right now they are "locally verified, hosted-pending," which is a real
   but different claim than "done and proven."
4. **(#16, #24) The bug hunt is open-ended by its own nature.** Nothing here should be read as
   "no bugs remain" — only that a real, evidenced, continuing pass happened this session. A
   fresh pass is always in scope for a future task.

## How this audit was produced

Read (not edited, except this file): `HANDOFF.md`, `ROADMAP.md`, `docs/render-in-actions.md`,
the relevant `.github/workflows/*.yml`, `design/docs/decisions.md`, and roughly 40 source and
test files across `design/packages/ui/src`, `design/packages/app/src`, and
`design/packages/render-actions/src`, chosen by following each of the 26 requests to its
implementation rather than trusting a commit message alone. Ran, locally, from `design/`:
`pathFieldPolicy.test.ts`, `configExplainCoverage.test.ts`, `noticeBulk.test.ts`,
`tabGroupPickerMount.test.ts`, `cursorAffordance.test.ts`, `speedLevels.test.ts`,
`githubAccountsStore.test.ts`, `DockedSurface.resize.test.ts`, `dockScrollChain.test.ts`,
`menuCoverage.test.ts`, and `regexPolicy.test.ts` — **11 files, 142 tests, 0 failures**. Did
not run the full ~7,400-test suite (too slow for this audit's scope and already reported at
478/478 files clean in `HANDOFF.md`'s own newest entry); did not wait for the hosted CI
backlog to clear. Checked `gh issue list --state all` (34 issues, all closed) and
`gh run list --branch main` for hosted CI state. Checked `agent-global-memory`'s `git log`
and remote-tracking status directly for item 26.
